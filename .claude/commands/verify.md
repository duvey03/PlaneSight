---
description: Adversarially verify an empirical/geological claim with the skeptic (fresh, unanchored)
---
Launch the `skeptic` subagent to adversarially verify the following claim. Give it ONLY
the claim and pointers to the evidence (scripts, data, output files, a branch name) — do
**NOT** summarize our prior discussion or how the result was framed, so it stays
unanchored and re-derives independently.

Claim + evidence pointers:
$ARGUMENTS

When it returns, relay its verdict verbatim. If it found a real problem (OVERSTATED /
REFUTED), propose the fix or the corrected claim to record on the relevant bead; if
NEEDS-HUMAN-EYES, surface exactly what the geologist must look at.
